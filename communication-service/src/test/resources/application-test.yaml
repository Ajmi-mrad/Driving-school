# Profil "test" : surcharge UNIQUEMENT ce qui doit l'être (fusionné par-dessus application.yaml).
# La datasource est fournie par Testcontainers (@ServiceConnection) ; le jwk-set-uri du resource
# server vient de application.yaml (décodeur paresseux, aucun appel réseau au chargement du contexte).
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false